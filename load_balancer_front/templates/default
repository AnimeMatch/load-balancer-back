upstream front_api {
    server ${FRONT1};
    server ${FRONT2};
}

server {
    listen 80 default_server;
    listen [::]:80 default_server;

    location ^~ / {
        proxy_pass http://front_api/;
        proxy_set_header Host $http_host;
    }
}